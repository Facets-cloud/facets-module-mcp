[project]
name = "facets-mcp-server"
version = "0.1.0"
description = "MCP server for Facets"
authors = [
    { name = "Anshul Sao", email = "anshul@example.com" }
]
dependencies = [
    "mcp[cli]",
    "checkov",
    "questionary",
    "ftf-cli @ git+https://github.com/Facets-cloud/module-development-cli.git@b24314afbd5e7fc8183a0ca7d28ca4909303f6d5",
    "swagger_client @ git+https://github.com/Facets-cloud/control-plane-python-sdk.git@e84a0ed323ba8fd193060a23bbda76023ca43782"
]
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.uv]
# (optional section for uv settings — no problem keeping this here)
