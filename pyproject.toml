[project]
name = "facets-mcp-server"
version = "0.1.0"
description = "MCP server for Facets"
authors = [
    { name = "Anshul Sao", email = "anshul@example.com" }
]
dependencies = [
    "mcp[cli]",
    "checkov",
    "questionary",
    "ftf-cli @ git+https://github.com/Facets-cloud/module-development-cli.git@d4daa586336933834613166440626d470ed48d00",
    "swagger_client @ git+https://github.com/Facets-cloud/control-plane-python-sdk.git@f15fe8236a7669340a7152487f2d5adbf145a41d"
]
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.uv]
# (optional section for uv settings — no problem keeping this here)
